<% @videos.each do |video| %>
    <%= cl_video_tag(video.file.file.public_id,:loop => true, :controls => true, :autoplay => true, :width=>500, :crop=>"scale", style: "border-radius: 20px;") %>

  <% end %>
